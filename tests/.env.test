# Test environment configuration
# Copy this to .env.test.local and fill in real values

API_BASE_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Super admin credentials (for /api/admin/* endpoints)
SUPER_ADMIN_EMAIL=admin@cataract.com
SUPER_ADMIN_PASSWORD=admin

# Clinic admin credentials (for patient/doctor endpoints)
ADMIN_EMAIL=deepika@gmail.com
ADMIN_PASSWORD=deepika

# Test clinic (must exist and be active)
TEST_CLINIC_ID=garuda-clinic

# Known test patient in the clinic
TEST_PATIENT_PHONE=6666666666

# Test patient phone prefix (tests create patients with 999000XXXX numbers)
TEST_PATIENT_PHONE_PREFIX=999000

# Backend must have DEV_MODE=true for OTP to be returned in response
DEV_MODE=true

# For AI evaluation layer (uses same key as backend)
# GOOGLE_API_KEY is read from backend/.env if not set here
